// ignore_for_file: constant_identifier_names

enum WidgetType { TWO_IMAGE, CHAT, DEFAULT, ORDER }

enum Sound { DEFAULT, BABY_GAME }

enum ChannelImportance { IMPORTANCE_LOW, IMPORTANCE_DEFAULT, IMPORTANCE_HIGH }

enum WidgetStyle { BIG_IMAGE, BIG_TEXT, DEFAULT }

enum LockScreenVisibility {
  VISIBILITY_PUBLIC,
  VISIBILITY_PRIVATE,
  VISIBILITY_SECRET
}
